{
  "name": "nova-assistant",
  "version": "1.0.0",
  "description": "Frameless Windows AI assistant UI (Electron)",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --enable-logging",
    "postinstall": "electron --version || true"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "openai": "^6.38.0"
  },
  "devDependencies": {
    "electron": "^31.0.0"
  }
}
